# 第 3 课学习完成：测试时计算扩展通过

**日期**：2026-08-31
**主题**：第 3 课（测试时计算扩展：pass@k / 10@k / best-of-n / 聚类 / LLM Monkeys 扩展规律）quiz 5/5。

## 结果

- **quiz：5/5**（通过线 4/5）
- **作业**：用户未提交「瓶颈在哪」分析题即推进下一课——按已记录的节奏偏好处理：不阻断，温和提示可回头补做。此类跳练行为已出现三次（第 1 课 quiz、贴标签作业、第 3 课作业），下一次若再跳过，调整为 quiz 优先节奏。
## 作业补交（2026-08-31）

- 第 1 题（瓶颈在哪）：✅ **满分**——答「模型筛选能力弱」：pass@1M=42% 证明生成/覆盖 OK（搜索空间有解），10@1M=30% 说明从 1M 策展出 10 个正确解的能力不足。判断链完整（生成端 vs 筛选端分离）。
- 第 2 题（两个改进方案）：用户忘记未答。已补讲：
  - 方案 1：**语义聚类**（AlphaCode 式：按「语法不同行为相同」聚类，每簇挑代表提交）——代价：需训练测试输入生成模型/语义嵌入器，额外算力与实现复杂度
  - 方案 2：**增强验证器重排**（PRM/规则验证器 best-of-n 取 top）——代价：验证器需数据训练，且存在噪声/被 hack 风险
  - 补充说明：加采样不是主方向（pass@k 已高 = 生成端不是瓶颈，再加采收益递减）。

## 第 4 课小作业（2026-09-01）——第 4 课已完成并交作业：quiz 仍待报分

- Q1 选型：用户只答「查响应长度」——✅ 答对一半；缺「全对样本占比」（准确率=1 比例 → 判采样量/奖励饱和探针）。完整诊断链：长度膨胀=reward hacking → token 级损失/过长惩罚；全对占比低=信号稀疏 → 动态采样；占比较高但无改善 → 奖励饱和。
- Q1 ② DAPO 方向：补讲 token 级损失 + 动态采样（对应上述两条诊断）。
- Q2 辨真伪：结论「不能」✅；**理由需修正**——用户理由「模型能自己先做对然后教自己」描述的恰是训练时扩展**能做的事**（搜索空间内解被固化），而题目问「从未见过的新题型」= 搜索空间里没有正确解 → 测时搜索搜不出 → 过滤不出 → 闭环无法启动。核心：训练时扩展放大已有能力，不创造边界外的能力；课程原话「无法教它解决全新问题或大幅领域外泛化，仍是开放问题」。

## 课间迁移问答（重点）

用户主动提问：**Claude Code / Codex / DeepSeek Harness / API 里哪些地方是测试时扩展**——高水平迁移问题，展示出将课程概念映射到真实产品的兴趣。回答框架：

- **判别卡**：不动权重 + 为质量多烧推理时算力
- Claude Code：agentic 重试循环（Reflexion 式）、plan mode 多轮规划、按需子代理并行 ✅；⚠️ 教训写回 CLAUDE.md 是**上下文位点跨任务演化**（inter-test-time）而非测试时扩展
- Codex/OpenAI：`reasoning_effort`（o3 系列 low/medium/high）把测试时计算预算显式产品化、代码执行反馈循环、子代理（scout）并行竞争
- DeepSeek R1：数学任务 pass@k 多采样（数值以论文为准，未在会话内核实具体数）[INFERENCE]
- 当前 harness（Oh My Pi）：并行工具调用、task 队列并行子代理、失败重试——可见行为层面即为 TTCS 组织形态 [INFERENCE，无内部实现一手材料]
- API 通用：推理预算参数（深度扩展）vs 并行采样 n/logprobs（广度扩展）
- **陷阱提醒**：判别法「关掉会话改动还在吗？在=演化，不在=测试时扩展」

## 影响后续

- 迁移讨论确认：用户有真实产品语境，后续课程可多给「映射到你的工具链」示例；第 4 课（训练时扩展）备课时可将「把教训写回 CLAUDE.md」作为上下文演化的例子继续用。
- 用户对「测试时扩展 vs 演化」的边界已用案例巩固——第 4 课闭环（训练时扩展）正好衔接：改动「回到权重里」= 跨任务的持久修改。
